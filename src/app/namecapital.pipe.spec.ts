import { NamecapitalPipe } from './namecapital.pipe';

describe('NamecapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new NamecapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
